<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.github.zluo01</groupId>
    <artifactId>vault</artifactId>
    <version>0.6.0</version>
    <packaging>jar</packaging>
    <name>Vault</name>
    <description>Manager for movies, TV shows and comics</description>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
        <maven-shade-plugin.version>3.6.2</maven-shade-plugin.version>
        <maven-surefire-plugin.version>3.5.6</maven-surefire-plugin.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <spotless-maven-plugin.version>3.6.0</spotless-maven-plugin.version>
        <maven-jar-plugin.version>3.5.1</maven-jar-plugin.version>
        <maven-dependency-plugin.version>3.8.1</maven-dependency-plugin.version>
        <jpackage-maven-plugin.version>1.8.0</jpackage-maven-plugin.version>
        <exec-maven-plugin.version>3.5.0</exec-maven-plugin.version>
        <maven-antrun-plugin.version>3.1.0</maven-antrun-plugin.version>

        <!-- sqlite-jdbc native dir to keep when packaging; overridden per platform
             by the natives-* profiles. Default ** keeps everything, so an
             unrecognized platform gets a working (fat) jar rather than a broken one. -->
        <sqlite.native.keep>**</sqlite.native.keep>

        <javafx.version>25.0.3</javafx.version>
        <controlsfx.version>11.2.3</controlsfx.version>
        <sqlite.version>3.53.2.0</sqlite.version>
        <caffeine.version>3.1.8</caffeine.version>
        <log4j.version>2.26.0</log4j.version>
        <jackson.version>3.2.2</jackson.version>

        <junit-jupiter.version>6.1.0</junit-jupiter.version>
        <mockito.version>5.18.0</mockito.version>

        <main.class>Launcher</main.class>

        <!-- https://www.panteleyev.org/javadoc/jpackage-maven-plugin/jpackage-mojo.html#type -->
        <app.type>APP_IMAGE</app.type>
        <!-- Linux/default icon; the mac/windows natives-* profiles override with .icns/.ico. -->
        <app.icon>${project.basedir}/assets/icons/vault.png</app.icon>
        <!-- Temporary workaround on macOS version restriction until reach version 1.0. -->
        <app.version>${project.version}</app.version>
    </properties>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>${project.basedir}/native/out/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <configuration>
                    <configLocation>configuration/checkstyle.xml</configLocation>
                </configuration>
                <dependencies>
                    <!-- The plugin's bundled checkstyle (9.3) cannot parse record patterns. -->
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>10.26.1</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-maven-plugin.version}</version>
                <configuration>
                    <java>
                        <googleJavaFormat/>
                        <removeUnusedImports/>
                        <formatAnnotations/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>${maven-clean-plugin.version}</version>
            </plugin>
            <!-- https://javadoc.io/doc/org.mockito/mockito-core/latest/org.mockito/org/mockito/Mockito.html#0.3 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven-dependency-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <!-- https://javadoc.io/doc/org.mockito/mockito-core/latest/org.mockito/org/mockito/Mockito.html#0.3 -->
                    <argLine>-javaagent:${org.mockito:mockito-core:jar} --enable-native-access=ALL-UNNAMED</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${main.class}</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven-jar-plugin.version}</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Implementation-Version>${project.version}</Implementation-Version>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-bom</artifactId>
                <version>${log4j.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.controlsfx</groupId>
            <artifactId>controlsfx</artifactId>
            <version>${controlsfx.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>${caffeine.version}</version>
        </dependency>
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-jul</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!-- Auto-activated by build platform: which sqlite-jdbc native dir survives packaging. -->
        <profile>
            <id>natives-linux-x86_64</id>
            <activation>
                <os>
                    <name>Linux</name>
                    <arch>amd64</arch>
                </os>
            </activation>
            <properties>
                <sqlite.native.keep>Linux/x86_64</sqlite.native.keep>
            </properties>
        </profile>
        <profile>
            <id>natives-linux-aarch64</id>
            <activation>
                <os>
                    <name>Linux</name>
                    <arch>aarch64</arch>
                </os>
            </activation>
            <properties>
                <sqlite.native.keep>Linux/aarch64</sqlite.native.keep>
            </properties>
        </profile>
        <profile>
            <id>natives-mac-aarch64</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>aarch64</arch>
                </os>
            </activation>
            <properties>
                <sqlite.native.keep>Mac/aarch64</sqlite.native.keep>
                <app.icon>${project.basedir}/assets/icons/vault.icns</app.icon>
                <app.version>1.0</app.version>
            </properties>
        </profile>
        <profile>
            <id>natives-mac-x86_64</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>x86_64</arch>
                </os>
            </activation>
            <properties>
                <sqlite.native.keep>Mac/x86_64</sqlite.native.keep>
                <app.icon>${project.basedir}/assets/icons/vault.icns</app.icon>
                <app.version>1.0</app.version>
            </properties>
        </profile>
        <profile>
            <id>natives-windows-x86_64</id>
            <activation>
                <os>
                    <family>windows</family>
                    <arch>amd64</arch>
                </os>
            </activation>
            <properties>
                <sqlite.native.keep>Windows/x86_64</sqlite.native.keep>
                <app.icon>${project.basedir}/assets/icons/vault.ico</app.icon>
            </properties>
        </profile>
        <profile>
            <id>uber</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>${maven-shade-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <createDependencyReducedPom>false</createDependencyReducedPom>
                                    <transformers>
                                        <transformer
                                                implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <mainClass>${main.class}</mainClass>
                                            <manifestEntries>
                                                <Implementation-Version>${project.version}</Implementation-Version>
                                                <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
                                            </manifestEntries>
                                        </transformer>
                                        <transformer
                                                implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                    </transformers>
                                    <filters>
                                        <filter>
                                            <artifact>*:*</artifact>
                                            <excludes>
                                                <exclude>module-info.class</exclude>
                                                <exclude>META-INF/*.SF</exclude>
                                                <exclude>META-INF/*.DSA</exclude>
                                                <exclude>META-INF/*.RSA</exclude>
                                            </excludes>
                                        </filter>
                                    </filters>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>app</id>
            <build>
                <plugins>
                    <!-- https://www.panteleyev.org/javadoc/jpackage-maven-plugin/examples/dependencies.html -->
                    <plugin>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <outputDirectory>${project.build.directory}/mods</outputDirectory>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <version>${maven-dependency-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>copy-dependencies</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-dependencies</goal>
                                </goals>
                                <configuration>
                                    <includeScope>runtime</includeScope>
                                    <outputDirectory>${project.build.directory}/mods</outputDirectory>
                                    <!-- Compile-time annotation jars leaked from caffeine; never loaded at runtime. -->
                                    <excludeArtifactIds>checker-qual,error_prone_annotations</excludeArtifactIds>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- sqlite-jdbc ships natives for every OS/arch (~12 MB); keep only the
                         build platform's (sqlite.native.keep, set by the natives-* profiles). -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>${maven-antrun-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>strip-sqlite-natives</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <zip destfile="${project.build.directory}/mods/sqlite-jdbc-stripped.jar">
                                            <zipfileset
                                                    src="${project.build.directory}/mods/sqlite-jdbc-${sqlite.version}.jar"
                                                    excludes="org/sqlite/native/**"/>
                                            <zipfileset
                                                    src="${project.build.directory}/mods/sqlite-jdbc-${sqlite.version}.jar"
                                                    includes="org/sqlite/native/${sqlite.native.keep}/**"/>
                                        </zip>
                                        <move file="${project.build.directory}/mods/sqlite-jdbc-stripped.jar"
                                              tofile="${project.build.directory}/mods/sqlite-jdbc-${sqlite.version}.jar"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>${jpackage-maven-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>verify</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <name>Vault</name>
                            <appVersion>${app.version}</appVersion>
                            <input>${project.build.directory}/mods</input>
                            <mainJar>${project.build.finalName}.jar</mainJar>
                            <mainClass>${main.class}</mainClass>
                            <type>${app.type}</type>
                            <icon>${app.icon}</icon>
                            <destination>${project.build.directory}/dist</destination>
                            <removeDestination>true</removeDestination>
                            <!-- Minimal runtime: jdeps + dynamic usage (java.logging for the JUL bridge, java.xml for
                                 NFO/log4j config parsing, jdk.charsets for NFO files in non-default encodings). -->
                            <addModules>
                                <module>java.base</module>
                                <module>java.desktop</module>
                                <module>java.sql</module>
                                <module>java.logging</module>
                                <module>java.xml</module>
                                <module>jdk.charsets</module>
                            </addModules>
                            <jLinkOptions>
                                <jLinkOption>--strip-debug</jLinkOption>
                                <jLinkOption>--no-man-pages</jLinkOption>
                                <jLinkOption>--no-header-files</jLinkOption>
                                <jLinkOption>--strip-native-commands</jLinkOption>
                            </jLinkOptions>
                            <javaOptions>
                                <javaOption>-Xmx512m</javaOption>
                                <javaOption>--enable-native-access=ALL-UNNAMED</javaOption>
                            </javaOptions>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
